// OWNER: 02-content-data.md — do not edit from another role
// Stub authored in Wave 0 (role 01) only to keep the tree compiling. Role 02 fills this with the
// real dormancy-backtracking pipeline graph from Wave 1 onward — see
// ../docs/backtracking-high-level.md, ../docs/backtracking-architecture.md, and
// workflow/02-content-data.md Contract out (archEdges[].order encodes the `simulate` sequence).
import type { ArchEdge, ArchNode } from "@/lib/types";

export const archNodes: ArchNode[] = [
  {
    id: "ops-portal",
    label: "Ops / Admin Portal",
    responsibility: "Submit dormancy backtracking jobs and monitor their progress",
    tech: "External portal, HTTPS client to ALB",
    scaling: "Managed externally; rate-limited at ALB to prevent queue saturation",
    failureModes: "Portal outage does not affect running jobs; only feedback to ops is lost; ALB enforces rate limits to prevent job queue overwhelming",
    observability: "Portal audit logs and HTTP access logs at ALB and CloudWatch",
    tradeoff: "Portal is an external system; this pipeline is resilient to portal unavailability but cannot notify ops of completion if portal is down",
    service: "External — Ops Portal",
  },
  {
    id: "alb",
    label: "ALB (internal, service.backend)",
    responsibility: "Route HTTPS traffic from Ops portal to API pods; enforce rate limiting and health checks",
    tech: "AWS Application Load Balancer, internal VPC, HTTPS termination, health checks every 30s",
    scaling: "Auto-scales managed by AWS; targets EKS service endpoint which distributes to live API pods",
    failureModes: "ALB failure → no new requests reach API (existing jobs unaffected); unhealthy pod → traffic routed to healthy replicas only",
    observability: "CloudWatch ALB metrics (RequestCount, TargetLatency, UnHealthyHostCount); X-Ray traces if instrumented",
    tradeoff: "ALB adds 5–10ms latency per inbound request and introduces a single entry point; mitigated by AWS-managed redundancy and cross-AZ deployment",
    service: "AWS ALB",
  },
  {
    id: "api-pod",
    label: "API Pod (Express, Node.js)",
    responsibility: "Accept dormancy job requests (POST /v1/dormancy-jobs with CSV body); return job status; handle retry/cancel/completion endpoints",
    tech: "Node.js Express, 2–3 pod replicas in EKS, IRSA role-bound for AWS API calls, pm2 process management",
    scaling: "Vertical: up to 4vCPU/8GB per pod; Horizontal: replicaset typically 2–3, scales on CPU > 70%",
    failureModes: "Pod OOM → Kubernetes evicts pod, traffic fails over to replicas; pod crash → Kubernetes restarts, in-flight request lost; RDS connection pool exhaustion → new requests receive 500",
    observability: "Application stdout logs → CloudWatch Logs; Prometheus metrics (http_request_duration_ms, api_errors_total)",
    tradeoff: "No dedicated message broker between API and worker; work is queued in RDS via SELECT...FOR UPDATE SKIP LOCKED polling. This avoids SQS/Kafka dependency for job assignment, but polling adds 2–10s latency vs event-driven. Mitigated by adaptive poll frequency (10s idle, 250ms busy).",
    service: "AWS EKS Pod — API",
  },
  {
    id: "worker-pod",
    label: "dormancy-worker Pod (Node.js pm2)",
    responsibility: "Poll RDS for pending dormancy rows; claim them atomically; request product version upgrades from Core Banking; stamp completion timestamps",
    tech: "Node.js pm2 process, single pod per EKS deployment, MAX_INFLIGHT 30 (global), per-job concurrency 5–25, tunable poll intervals (IDLE 10s, ACTIVE 2s, BUSY 250ms)",
    scaling: "Horizontal: more pod replicas → more concurrent claims; per-pod in-memory jobSlots map and global MAX_INFLIGHT prevent runaway growth",
    failureModes: "Worker crash mid-claim → row left IN_FLIGHT; sweep detects stale upgradeAttemptedAt, re-enqueues to PENDING; Core Banking API timeout → row stays IN_FLIGHT, retried on next poll; DB connection loss → logs ERROR, reconnects and continues polling",
    observability: "Application logs (pollLoop ticks, claim batch counts, upgrade outcomes); per-row attemptCount in RDS; CloudWatch custom metrics for throughput",
    tradeoff: "DB-as-queue means no desync between job table and queue state, but polling introduces latency and database load (1–10 SELECT FOR UPDATE queries/sec); no way to fanout to multiple workers except via horizontal scaling of pods",
    service: "AWS EKS Pod — Worker",
  },
  {
    id: "kafka-consumer-pod",
    label: "dormancy-kafka-consumer Pod (Node.js Kafka consumer)",
    responsibility: "Consume account-update events from the Core Banking Kafka topic; confirm completed upgrades; sync activity dates and AML status; publish to SQS for downstream relay",
    tech: "Node.js librdkafka Kafka consumer, eachBatch handler, graceful draining on shutdown, control-poll loop runs every 10s independently for grace-window logic and finalization",
    scaling: "Horizontal: more pod replicas → wider Kafka partition assignment; partition count determines max parallelism (topic typically 12–24 partitions)",
    failureModes: "Kafka broker down → consumer pauses (buffered offsets) until broker recovers; unhandled event → logged as ERROR, offset committed to avoid reprocessing; SQS full → exponential backoff retries for publish",
    observability: "Consumer lag (Kafka offset lag exporter or custom CloudWatch metric); batch processing time; completion event counts; SQS publish latency; distributed traces",
    tradeoff: "Kafka rebalancing pauses consumption for 10–30s during pod failures or scaling (unavoidable with Kafka); mitigated by independent control-poll loop that handles grace-window and finalization without blocking on eachBatch",
    service: "AWS EKS Pod — Kafka Consumer",
  },
  {
    id: "rds-postgres",
    label: "RDS Postgres (migration_db)",
    responsibility: "Persist dormancy jobs, rows, claim state, attempt counts, timestamps, and audit trail; serve as work queue via SELECT...FOR UPDATE SKIP LOCKED",
    tech: "PostgreSQL 14+, single primary + read replica (async replication), IOPS-optimized storage (gp3, 3000 IOPS baseline), encrypted at rest (KMS), automated backups every 24h, 35-day retention",
    scaling: "Vertical: larger instance type (e.g. db.r6i.2xlarge → db.r6i.4xlarge) for higher connection count and throughput; read replica handles audit/report queries",
    failureModes: "Primary failure → automatic failover to replica (30–60s RTO, ~5min RPO due to async replication); connection pool exhaustion (> max_connections) → clients see 'too many connections' error; disk full → writes rejected; replication lag → replica read-after-write inconsistency",
    observability: "CloudWatch RDS metrics (DatabaseConnections, CPUUtilization, NetworkReceiveThroughput, ReplicationLag); slow query log (queries > 1000ms); enhanced monitoring (OS-level metrics)",
    tradeoff: "Async replication adds replication lag risk (typically < 100ms, worst case ~5s); writes immediately visible to primary, but replica read-after-write requires careful application logic or routing immediate reads to primary",
    service: "AWS RDS (PostgreSQL)",
  },
  {
    id: "s3",
    label: "S3 (manifest / failed / completed CSVs)",
    responsibility: "Store job manifest (JSON), completed accounts (CSV), and failed accounts (CSV) as final, immutable job output",
    tech: "S3 bucket with versioning enabled, server-side encryption (SSE-S3), lifecycle policies archive to Glacier after 90 days, object lock disabled",
    scaling: "Unlimited (S3 scales horizontally behind the scenes); per-object request rate in the thousands",
    failureModes: "S3 API throttling (extremely rare in single-account AWS) → SDK retries with exponential backoff; network partition → put_object and get_object timeout with 5-minute retry logic",
    observability: "S3 access logs (optional, written to separate logging bucket); CloudTrail API calls; CloudWatch request metrics (optional, costs money)",
    tradeoff: "S3 eventual-consistency model (now strong-read-after-write) is acceptable for async job output; latency is 50–200ms for puts and 10–100ms for gets; objects are immutable once written (no in-place updates)",
    service: "AWS S3",
  },
  {
    id: "sqs-queue",
    label: "SQS (dormancy-sync status queue)",
    responsibility: "Buffer dormancy-sync status notifications before relay to the BE Orchestrator; decouple Kafka consumer from orchestrator availability",
    tech: "SQS standard queue (not FIFO), message retention 1 day (86400s), visibility timeout 30s, DLQ configured for unparseable messages",
    scaling: "Unlimited throughput (SQS auto-scales); no per-account quota concerns in single AWS account",
    failureModes: "SQS throttling (rare) → SDK retries; message body exceeds 256KB → rejected by SQS, sent to DLQ; BE Orchestrator unavailable → queue builds up (harmless, eventual consistency)",
    observability: "CloudWatch SQS metrics (ApproximateNumberOfMessagesVisible, NumberOfMessagesSent, NumberOfMessagesReceived); DLQ depth monitored for alarm trigger",
    tradeoff: "SQS adds 100–500ms latency before the BE Orchestrator relay (async handoff); benefit is decoupling: Kafka consumer is never blocked waiting on orchestrator HTTP calls, and SQS is more durable than in-memory queue",
    service: "AWS SQS",
  },
  {
    id: "tm-core-banking",
    label: "Core Banking",
    responsibility: "Execute product version upgrades on demand; emit account-update completion events; provide account data, transaction history, and AML status",
    tech: "Core Banking REST API (synchronous upgrade calls) + Kafka topic (asynchronous account-update confirmations)",
    scaling: "N/A — external third-party service with its own SLA; this pipeline respects Core Banking's rate limits and retry policies",
    failureModes: "Core Banking API 503 or timeout (> 30s) → worker treats as retryable, applies exponential backoff with max 5 attempts; Kafka topic lag → consumer pauses, grace window allows up to 30min for confirmation before fallback re-verify",
    observability: "Core Banking provides API response codes and times; application logs call outcomes (success, timeout, error); Kafka lag monitored via consumer group lag",
    tradeoff: "External dependency means this pipeline is subject to Core Banking's availability and rate limits; mitigated by retry logic, graceful degradation (grace window + fallback verify directly against Core Banking), and design choices to batch upgrades",
    service: "External — Core Banking",
  },
  {
    id: "ud-mesh",
    label: "BE Orchestrator",
    responsibility: "Receive dormancy-sync status notifications via SQS and propagate internally across the organization",
    tech: "BE Orchestrator message broker / service bus, SQS-driven ingestion (outbound-only from this pipeline's perspective)",
    scaling: "N/A — external to this system",
    failureModes: "BE Orchestrator unavailable → SQS queue builds up (harmless), application continues; messages expire from SQS after 1-day retention period (acceptable, eventual consistency)",
    observability: "BE Orchestrator delivery confirmation (if available); SQS metrics show queue depth and messages received",
    tradeoff: "Fire-and-forget async notification via SQS means BE Orchestrator delivery is not guaranteed in real-time; acceptable for dormancy status (eventual consistency OK, and orchestrator unavailability doesn't block job completion)",
    service: "External — BE Orchestrator",
  },
];

export const archEdges: ArchEdge[] = [
  // Happy-path sequence for the `simulate` animation and the sequence-diagram view.
  { from: "ops-portal", to: "alb", order: 1, label: "Submit CSV of dormant accounts (HTTPS)" },
  { from: "alb", to: "api-pod", order: 2, label: "Route request" },
  { from: "api-pod", to: "rds-postgres", order: 3, label: "Persist job + rows" },
  { from: "api-pod", to: "s3", order: 4, label: "Write job manifest" },
  { from: "worker-pod", to: "rds-postgres", order: 5, label: "Claim pending rows (SKIP LOCKED)" },
  { from: "worker-pod", to: "tm-core-banking", order: 6, label: "Request product version upgrade" },
  { from: "tm-core-banking", to: "kafka-consumer-pod", order: 7, label: "account-update event (async)" },
  { from: "kafka-consumer-pod", to: "rds-postgres", order: 8, label: "Confirm + sync activity date" },
  { from: "kafka-consumer-pod", to: "tm-core-banking", order: 9, label: "Sync AML status" },
  { from: "kafka-consumer-pod", to: "s3", order: 10, label: "Write completed/failed CSVs" },
  { from: "kafka-consumer-pod", to: "sqs-queue", order: 11, label: "Publish dormancy status" },
  { from: "sqs-queue", to: "ud-mesh", order: 12, label: "Relay status notification" },
];
